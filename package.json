{
  "name": "pitchsense-server",
  "version": "1.0.0",
  "description": "PitchSense backend — proxies Gemini API calls to protect the API key",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
